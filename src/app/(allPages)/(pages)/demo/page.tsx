"use client";
import { useSelector } from "react-redux";
// import { selectUser } from "@/store/slice/userSlice";

const Demo = () => {
  async function listProperty(e: any) {
    e.preventDefault();
    // let getTitle = document.getElementById("listingTitle")?.value?.toString();

    // console.log(getTitle);
  }
  return <div>demo pageeeee</div>;
};

export default Demo;
